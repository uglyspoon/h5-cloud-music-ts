export type categoryType = {
  name: string;
  key: string;
};

export type alphaType = {
  key: string;
  name: string;
};

export type RankType = {
  [key: string]: string;
};

export type palyModeType = {
  sequence: number;
  loop: number;
  random: number;
};
